<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WisataMagelang" parent="Theme.Material3.DayNight">
        <!-- Primary Color -->
        <item name="colorPrimary">@color/cyan</item>
        <item name="colorOnPrimary">@color/cyanlight</item>
        <!-- Secondary Brand Color -->
        <item name="colorSecondary">@color/brown</item>
        <item name="colorOnSecondary">@color/brownlight</item>
        <!-- -->
    </style>

    <style name="Theme.WisataMagelang" parent="Base.Theme.WisataMagelang" />

    <style name="Theme.App.SplashScreenWisataMagelang" parent="Theme.SplashScreen" >
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_app2</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.WisataMagelang</item>
    </style>
</resources>